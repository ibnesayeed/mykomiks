<% div_for comment do %>
  <p>
    By: <strong><%= (comment.website.blank?) ? comment.author : link_to(comment.author, comment.website) %></strong> <%= time_ago_in_words(comment.created_at) %> ago<br />
    <%= comment.body %>
  </p>
<% end %>
